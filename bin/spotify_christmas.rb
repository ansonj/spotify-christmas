# frozen_string_literal: true

# Primary module, containing the interface and business logic
module SpotifyChristmas
  def self.run(sqlite_path)
    puts "Running with sqlite at '#{sqlite_path}'"
  end
end
